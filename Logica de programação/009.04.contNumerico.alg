algoritmo "contNUmerico"
var
    cont, soma, num: real
inicio
    cont <- 1
    enquanto (cont <= 5) faca
        escreva("Digite um valor? :")
        leia(num)
        soma <- soma + num
        cont <- cont + 1
    fimenquanto`
escreva("a soma de todos os valore foi de: ",soma)
fimalgoritmo
