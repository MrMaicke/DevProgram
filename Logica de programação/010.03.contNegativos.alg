algoritmo "contador de numeros negativos"
var
    num, cont : inteiro
inicio
    cont <- 1
    repita
        escreval("Digite um numero: ")
        leia(num)
        c <- c + 1
    ate (c > 5)
fimalgoritmo