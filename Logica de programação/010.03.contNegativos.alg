algoritmo "contador de numeros negativos"
var
    num, cont : inteiro
inicio
    cont <- 1
    repita
        escreva("Digite um numero: ")
        leia(num)
        c <- c + 1
    ate (cont > 5)
fimalgoritmo