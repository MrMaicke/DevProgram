algoritmo "Dependentes"
var
    nome, dep: inteiro
    sal: real

inicio
    escreva("qual o nome do funcio nario? ")
    leia (nome)
    escreva("qual o salario do funcionario? ")
    leia (sal)
    escreva("qual a quantidade de dependentes? ")
    leia (dep)


fimalgoritmo