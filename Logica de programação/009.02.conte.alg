algoritmo "conteAte10eate0"
var
    contador, valor: real
inicio
    contador <- 0
    escreva("até qual valor quer que eu conte? :")
    leia(valor)
    enquanto (contador <= valor) faca
        escreval(contador)
        contador <- contador + 1
    fimenquanto
fimalgoritmo