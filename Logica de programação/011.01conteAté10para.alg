algoritmo "contador"
var
    num1: inteiro
inicio
    para num1 <- 0 ate 10 passo 2 faca 
    //a primeira parte especifica de onde começa e a segunda faz a regra (soma) | sem necessidade do (passo)
        escreval (num1)
    fimpara
    //para num1 <- 1 ate 10 faca | conta até 10
    //para num1 <- 10 ate 1 faca | conta até 1
    //
    //
fimalgoritmo