algoritmo "conversor de temperatura"
var
    f, c: real
inicio
    escreva ("qual é a temperatura nos EUA? ")
    leia(f)
    c <- ((f - 32) / 1.8)
    escreva("a temperatura no brasil seria de: ", c:1:1)
fimalgoritmo
